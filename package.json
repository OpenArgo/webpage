{
  "name": "openargo-docs",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "start": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "deploy": "npm run build && wrangler pages deploy ./doc_build"
  },
  "dependencies": {
    "rspress": "^1.47.1",
    "react": "^18.0.0",
    "react-dom": "^18.0.0"
  },
  "devDependencies": {
    "@types/react": "^18.0.0",
    "@types/react-dom": "^18.0.0",
    "typescript": "^5.0.0",
    "wrangler": "^3.65.1"
  },
  "engines": {
    "node": ">=18.0"
  }
}
